export class Member {
    firstName: string;
    lastName: string;
    age: number;
    constructor(firstName, lastName, age) {
        this.firstName = firstName;
        this.lastName = lastName;
        this.age = age;
    }
}